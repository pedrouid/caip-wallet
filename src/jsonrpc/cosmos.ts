import { ChainJsonRpcRoutes } from '../helpers';

export const CosmosJsonRpc: ChainJsonRpcRoutes = {
  routes: {
    http: [],
    signer: [],
  },
  wallet: {
    accounts: '',
    auth: [],
  },
};
